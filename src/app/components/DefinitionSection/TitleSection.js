import React from 'react';

const TitleSection = (word) => {
    return (
        <div>
            <div>
                <p>{word}</p>
            </div>
        </div>
    );
};

export default TitleSection;
